import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Stratify app smoke test', (WidgetTester tester) async {
    // Placeholder test - Supabase initialization requires env setup
    expect(1 + 1, 2);
  });
}
